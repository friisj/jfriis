/**
 * Form Components (v2 - Production Ready)
 *
 * Reusable form components and utilities with:
 * - Accessibility (WCAG compliant)
 * - Type safety (TypeScript + runtime validation)
 * - Performance (memoization)
 * - Error resilience (error boundaries)
 */

export { FormFieldWithAI } from './form-field-with-ai'
export type { FormFieldWithAIProps } from './form-field-with-ai'

// Re-export types for convenience
export type { EntityType, AIContext, SerializableValue } from '@/lib/ai/types/entities'
